charm-crypto==0.1.3     # For pairing-based cryptography and ECC
cryptography==42.0.5    # For hashes and signatures
asyncio                 # Built-in, no need to install separately

# To install above dependencies:
# pip install -r requirements.txt